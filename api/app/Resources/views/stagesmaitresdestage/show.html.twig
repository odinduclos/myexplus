{% extends 'base.html.twig' %}

{% block body %}
    <h1>Stagesmaitresdestage</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stagesMaitresDeStage.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('stagesmaitresdestage_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('stagesmaitresdestage_edit', { 'id': stagesMaitresDeStage.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
