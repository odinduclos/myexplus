{% extends 'base.html.twig' %}

{% block body %}
    <h1>Stage</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stage.id }}</td>
            </tr>
            <tr>
                <th>Datedebut</th>
                <td>{{ stage.dateDebut }}</td>
            </tr>
            <tr>
                <th>Datefin</th>
                <td>{{ stage.dateFin }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('stages_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('stages_edit', { 'id': stage.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
